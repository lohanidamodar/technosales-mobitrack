apply plugin: 'com.android.application'

android {
    compileSdkVersion 23
    buildToolsVersion '23.0.1'

    defaultConfig {
        minSdkVersion 4
        targetSdkVersion 23
        versionCode 2
        versionName '1.0'
    }

    signingConfigs {
        release {
            storeFile file('/media/dlohani/personal/initiations/rl-projects/technologysales.jks')
            keyAlias 'mobitrack'
            storePassword '73chno$@les'
            keyPassword 'mob!7t@ck'
        }
    }

    productFlavors {
        regular {
            applicationId "com.technosales.mobitrack"
            buildConfigField "boolean", "HIDDEN_APP", "false"
        }
        hidden {
            applicationId "com.technosales.mobitrack.hidden"
            buildConfigField "boolean", "HIDDEN_APP", "true"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.release
        }
    }
}

dependencies {
    testCompile 'junit:junit:4.12'
    testCompile 'org.robolectric:robolectric:3.0'
    compile 'com.android.support:support-v4:23.1.1'
}


